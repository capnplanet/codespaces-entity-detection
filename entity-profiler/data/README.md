Data folders:

- `raw/`      - original video files or frame dumps (not committed)
- `interim/`  - intermediate tracklets or extracted frames
- `processed/`- serialized features or pre-built observations
